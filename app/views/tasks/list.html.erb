<h1>My Tasks</h1>

<% @tasks.each_with_index do |task, index| %>
  <%= "#{index + 1} | #{task.completed? ? '[x]' : '[ ]'}" %>
  <%= link_to "#{task.title}", task_path(task) %>
  <%= link_to "💣", task_path(task), method: :delete %>
  <br>
<% end %> <br>

<%=  link_to "Create new task", new_task_path %>
